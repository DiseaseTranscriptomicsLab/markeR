% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo_import_module.R
\name{.try_token_match}
\alias{.try_token_match}
\title{Internal: token-overlap match between expression column names and metadata values}
\usage{
.try_token_match(expr_cols, meta_vals, min_score = 2L)
}
\arguments{
\item{expr_cols}{Character vector of expression matrix column names.}

\item{meta_vals}{Character vector of values from one metadata column.}

\item{min_score}{Minimum token-length score to accept a match (default 3).}
}
\value{
List: idx (best meta index per expr col), scores, accepted (logical), n_matched.
}
\description{
Tokenises both sides by splitting on non-alphanumeric characters, then scores
each (expr_col, meta_val) pair by the summed length of shared tokens (≥2 chars).
Designed to handle cases like expr col "counts.sampA" vs meta value "library: sampA".
}
